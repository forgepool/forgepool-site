# Mixed Content Check

Gesucht wurde in `src/` und `public/` nach `http://`.

## Ergebnis

Keine echten Mixed-Content-Treffer gefunden.

## Hinweise

SVG-Namespace-Angaben wie `xmlns="http://www.w3.org/2000/svg"` sind normal und kein Mixed Content.

## Link-Checks

- `href="#"` Treffer in `src/`: 0
- `mailto:` Treffer in `src/`: 3

`mailto:` Treffer sind für direkte Kontakt-/Impressumslinks vorgesehen.
